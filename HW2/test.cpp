struct element {
    int col, row, val;
}
struct Matrix {
    vector<element> v;
} A, B;


void add(Matrix A, Matrix B) {
    Matrix C;
    map<pair<int,int>,int> mp;
    for (int i = 0; i < A.v.size(); i++) {
        mp[A.v[i].col, ]
    }
}

void mul (Matrix A, Matrix B) {
    for (int i = 0;)
}
(row, col) (row, col) = row, 